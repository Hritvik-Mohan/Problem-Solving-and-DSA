// "1. Given a stack S, you need to reverse the element in the stack using another empty stack S’ Input: 2,4,5,7,8 Output: 8,7,5,4,2"

function stackFun(s1, s2, n) {
    for(i = 0; i < n; i++) {
        let num = s1[s1.length - 1];
        s1.pop();
        s2.pus(temp);
    }
}

function stackReverse(s, n) {
    let s2 = [];
    for(let i = 0; i < i++) {
        let num = s[s.length - 1];
        s.pop();

        transfer(s, s2, n-i-1);
        s.push(x);
    }
}